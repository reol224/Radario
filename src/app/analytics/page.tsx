"use client";

import Link from "next/link";
import { useMemo, useState } from "react";

const history = [{ day: "Today", jobs: 37, low: 9 }, { day: "Yesterday", jobs: 42, low: 11 }, { day: "Aug 13", jobs: 31, low: 7 }, { day: "Aug 12", jobs: 28, low: 5 }, { day: "Aug 11", jobs: 46, low: 13 }];
const sources = [{ name: "Company sites", discovered: 16, applied: 3, interviews: 1 }, { name: "LinkedIn", discovered: 14, applied: 2, interviews: 0 }, { name: "Manual inbox", discovered: 7, applied: 1, interviews: 0 }];

export default function AnalyticsPage() {
  const [range, setRange] = useState("7");
  const multiplier = Number(range) / 7;
  const totals = useMemo(() => ({ discovered: Math.round(184 * multiplier), saved: Math.round(31 * multiplier), applied: Math.round(17 * multiplier), interviews: Math.round(3 * multiplier) }), [multiplier]);
  return <main className="min-h-screen bg-[#f7f8fa] p-6 text-slate-950 sm:p-10"><div className="mx-auto max-w-5xl"><Link href="/" className="text-sm font-bold text-[#d84320]">← Today&apos;s Radar</Link><div className="mt-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p className="text-xs font-black uppercase tracking-[0.18em] text-[#ff5b35]">Signal review</p><h1 className="mt-2 text-3xl font-black">Discovery analytics</h1><p className="mt-2 text-slate-500">See which sources and signals are producing opportunities worth your time.</p></div><label className="text-sm font-bold text-slate-500">Time range<select value={range} onChange={(event) => setRange(event.target.value)} className="ml-2 rounded-lg border border-slate-200 bg-white px-3 py-2 font-bold text-slate-800"><option value="7">Last 7 days</option><option value="30">Last 30 days</option><option value="90">Last 90 days</option></select></label></div><div className="mt-8 grid grid-cols-2 gap-3 md:grid-cols-4">{[[totals.discovered, "Discovered"], [totals.saved, "Saved"], [totals.applied, "Applied"], [totals.interviews, "Interviews"]].map(([value, label]) => <div key={label} className="rounded-2xl border border-slate-200 bg-white p-5"><p className="text-3xl font-black">{value}</p><p className="mt-1 text-sm text-slate-500">{label}</p></div>)}</div><div className="mt-5 grid gap-5 md:grid-cols-[1.2fr_0.8fr]"><section className="rounded-2xl border border-slate-200 bg-white p-6"><div className="flex items-center justify-between"><div><h2 className="font-black">Search history</h2><p className="mt-1 text-sm text-slate-500">New listings found per discovery run.</p></div><span className="rounded-full bg-orange-50 px-3 py-1 text-xs font-bold text-orange-800">Local demo data</span></div><div className="mt-5 space-y-4">{history.map((entry) => <div key={entry.day}><div className="flex justify-between text-sm"><span className="font-medium">{entry.day}</span><span className="font-bold">{entry.jobs} jobs · {entry.low} under 25 applicants</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-[#ff5b35]" style={{ width: `${Math.round((entry.jobs / 50) * 100)}%` }}/></div></div>)}</div></section><section className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="font-black">Competition</h2><dl className="mt-5 space-y-4 text-sm"><div className="flex justify-between"><dt className="text-slate-500">Average applicants</dt><dd className="font-black">18</dd></div><div className="flex justify-between"><dt className="text-slate-500">Median applicants</dt><dd className="font-black">12</dd></div><div className="flex justify-between"><dt className="text-slate-500">Under 25 applicants</dt><dd className="font-black text-[#d84320]">24%</dd></div><div className="flex justify-between"><dt className="text-slate-500">Applicant count unknown</dt><dd className="font-black">31%</dd></div></dl><p className="mt-5 rounded-lg bg-slate-50 p-3 text-xs leading-5 text-slate-500">Unknown counts are excluded from averages. Radario never treats missing applicant data as zero.</p></section></div><section className="mt-5 rounded-2xl border border-slate-200 bg-white p-6"><h2 className="font-black">Source performance</h2><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[560px] text-left text-sm"><thead className="border-b border-slate-100 text-xs uppercase tracking-wider text-slate-400"><tr><th className="pb-3">Source</th><th className="pb-3">Discovered</th><th className="pb-3">Applied</th><th className="pb-3">Interviews</th><th className="pb-3">Apply rate</th></tr></thead><tbody>{sources.map((source) => <tr key={source.name} className="border-b border-slate-100 last:border-0"><td className="py-4 font-bold">{source.name}</td><td className="py-4">{source.discovered}</td><td className="py-4">{source.applied}</td><td className="py-4">{source.interviews}</td><td className="py-4 font-bold">{Math.round((source.applied / source.discovered) * 100)}%</td></tr>)}</tbody></table></div></section><p className="mt-6 text-xs text-slate-400">Analytics become account-backed when discovery runs and application events are written to Supabase.</p></div></main>;
}
